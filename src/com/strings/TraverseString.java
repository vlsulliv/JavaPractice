package com.strings;

public class TraverseString {
    String str = "Hello, I am Vince";
    for (int = 0; i<str.length(); i++) {
        int index = 0;
        if(str.charAt(i) == str.charAt(i+1)){

        }
    }
}
